interface:
  display_name: "Dispatch Ready Issues"
  short_description: "Dispatch only safe, unblocked issue work"
  default_prompt: "Use $dispatch-ready-issues to analyze this repository and dispatch only currently unblocked implementation issues to subagents."
